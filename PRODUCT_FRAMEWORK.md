# QwertLearn 产品与扩展框架

> 文档版本：1.0｜基线日期：2026-09-07｜状态：字母小火车可玩纵向切片完成｜适用范围：产品设计、游戏开发、词库、音频、排行榜、数据与后续扩展

## 1. 文档权威与变更规则

本文件是 QwertLearn 的产品与架构基线。后续新增游戏、学习模式、词库、音频、排行榜或云端能力时，必须先确认是否符合本文件，再实施代码。

变更规则：

1. 不破坏“键位学习 → 单词抄写 → 词义回忆 → 听写复习”的学习主线。
2. 新游戏必须复用共享输入、词库、难度、进度、排行榜和音频服务，不能各自重复实现。
3. 涉及视觉和交互的新功能，先提供 2～3 套可比较原型，确认后再编码。
4. 改变核心规则、数据模型或扩展接口时，先更新本文件，并在“决策记录”中说明。
5. 音频素材未核验授权前不得提交仓库，也不得使用从商业游戏中提取的音乐或音效。
6. 每次扩展都必须保持旧词库、旧成绩和旧学习进度可兼容迁移。

## 2. 产品定位

QwertLearn 是面向小学阶段儿童的 Web 英语打字游戏集合，首要用户是：

- 小学四年级；
- 尚未熟悉键盘与盲打；
- 英语词汇基础较弱；
- 需要通过短时、重复、有反馈的游戏建立键位记忆和单词拼写能力。

产品不是单纯测试 WPM，而是同时解决三个问题：

1. 找得到字母键；
2. 能正确拼写单词；
3. 能将英文拼写与中文、图片和读音建立联系。

建议产品展示名：**QwertLearn 英语打字冒险岛**。

## 3. 不可变产品原则

### 3.1 儿童优先

- 反馈积极、简短，不羞辱、不恐吓。
- 失败使用泡泡救援、暂停追赶等温和表现，不表现溺水或暴力。
- 一次误触不能立即终止整局，初始使用生命值或短暂停顿容错。
- 儿童界面突出连击、星星和个人进步；WPM、错误热力图等详细数据放在家长中心。

### 3.2 准确优先于速度

- 新手先达到稳定正确率，再提高移动速度。
- 不同词库、模式、难度和速度等级不能混在同一排行榜。
- 长单词必须获得更长输入窗口，不能统一倒计时。

### 3.3 少量多次

建议单次练习 5～10 分钟：

1. 60～90 秒键位热身；
2. 3～5 分钟游戏；
3. 1～2 分钟错词回顾。

### 3.4 本地优先与未成年人保护

- MVP 默认不要求账号。
- 使用昵称和卡通头像，不收集真实姓名、学校、电话和公开位置。
- 学习进度、设置和家庭排行榜默认保存在本机。
- 公开排行榜和云同步属于后续可选能力，必须有家长控制和明确的数据说明。

## 4. 产品信息架构

### 4.1 首页：冒险地图

首页采用可扩展地图，每个游戏是一个区域：

- 键盘训练营：键位热身和手指提示；
- 池塘岛：青蛙跳荷叶；
- 城市岛：警察抓小偷；
- 词语花园：鼠标、触控或键盘选择英文与中文词义配对；
- 我的单词本：掌握、易错、待复习单词；
- 家长中心：词库、练习时长、正确率、薄弱键位和设置。

未来增加太空、海底、农场等游戏时，新增地图区域，不改变现有入口和学习主线。

### 4.2 首次使用流程

1. 选择本地昵称和头像；
2. 选择教材词库或体验词库；
3. 完成约 60 秒键位基线测试；
4. 推荐初始提示等级和游戏速度；
5. 进入键位热身，然后开始第一局。

## 5. 学习模式与递进

所有游戏共享以下模式：

| 模式 | 给孩子的线索 | 学习目标 |
|---|---|---|
| 键位模式 | 单个字母、屏幕键盘、手指提示 | 建立键盘位置记忆 |
| 抄写模式 | 完整英文、中文/图片可选 | 练习键位与拼写动作 |
| 回忆模式 | 中文或图片，不显示英文 | 主动回忆英文拼写 |
| 听写模式 | 英文读音，可重复播放 | 建立声音与拼写联系 |
| 复习模式 | 系统混合易错词 | 巩固薄弱词汇 |

提示逐步淡出：

1. 高亮下一个键和对应手指；
2. 只高亮键盘区域；
3. 显示屏幕键盘但不提示；
4. 隐藏屏幕键盘。

同一个单词连续出错时，不立即公布全部答案；先给首字母、音节或按键提示，再允许重试。

## 6. 游戏一：青蛙跳荷叶

### 6.1 核心循环

1. 青蛙位于当前荷叶或岸边；
2. 前方有 1～4 排横向移动的荷叶；
3. 当前可跳目标明显高亮；
4. 孩子输入目标单词；
5. 正确完成后青蛙按“底部 → 顶部”的固定顺序跳到目标荷叶，并持续跟随所在荷叶移动；到达当前最高荷叶后自动返回底部起点，再从最下层开始新一轮，禁止从顶部反向往下跳；
6. 每完成 8 个单词通过一个阶段，只显示短暂升级提示，不弹出结算打断游戏；
7. 阶段提升后增加荷叶排数、移动速度或缩短动态输入窗口，词序仍由共享随机选词服务补充；
8. 首次通过阶段 1 后解锁无尽挑战；无尽挑战持续跳跃，不设置固定通关词数；
9. 单词超时或荷叶离开安全区域记为一次失败，HUD 明确显示“失败次数 / 3”；普通错误按键只重置连击，不增加失败次数；
10. 普通学习与无尽挑战均固定最多 3 次失败；第 3 次失败立即停止并结算总跳跃数、最高阶段、正确率、积分和错词，孩子主动结束或页面退出也会结算。

### 6.2 输入反馈

- 正确字母变绿并播放轻微正确音；
- 错误字母变红，青蛙保持原位，播放柔和错误音；
- 完成单词后立即播放单词读音和跳跃音效；
- 连击达到 5、10、20 时给出视觉和短音效奖励；
- 游戏不会因为一次误触立即结束。

### 6.3 难度递进

| 等级 | 荷叶 | 词汇 | 提示 |
|---|---|---|---|
| 1 | 单排、静止或极慢 | 单字母、2～3 字母词 | 完整键位提示 |
| 2 | 单排缓慢移动 | 3～4 字母词 | 键盘区域提示 |
| 3 | 两排反向移动 | 3～5 字母词 | 仅显示英文 |
| 4 | 三至四排、速度变化 | 教材词汇 | 中文/图片回忆 |
| 5 | 组合速度与特殊荷叶 | 易错词与听写 | 无键位提示 |

无尽挑战规则：

- `speedTier` 仍保持 1～5，用于提示强度、词汇难度和排行榜隔离；新增无上限 `stageLevel` 表示本局挑战阶段；
- 每 8 个完成词提升一个 `stageLevel`，阶段提升在游戏内以 1～2 秒横幅提示，不中断输入流程；
- 阶段 1～4 逐步增加到四排荷叶；阶段 5 以后保持最多四排，并继续提高移动速度、方向组合和时间压力；
- 高阶段输入窗口仍按单词长度计算并保留绝对下限，不能让长词与短词共享同一不公平倒计时；
- 无尽局内难度只升不降；低正确率只影响下一局推荐起点和提示，不在当前挑战中突然减速；
- “无尽”仅表示不设置固定通关词数，不代表无限容错；无尽挑战同样在累计第 3 次失败时强制结束；
- 系统持久化最高解锁阶段、单局最高阶段和历史最长连跳；孩子可从热身阶段或已解锁检查点开始。

动态时间建议：

`可用时间 = 基础反应时间 + 单词长度 ÷ 近期字符速度 × 容错系数`

结果限制在合理上下限内，保证长词可完成，也避免低难度无限等待。

体验词库的 1 级速度在首个有效按键后才启动倒计时，每词至少提供 18 秒、最多 30 秒；页面失焦和游戏暂停不消耗可用时间。

### 6.4 排行榜

排行榜键由以下条件共同组成：

`游戏 + 学习模式 + 词库 + 难度 + 速度等级`

主要成绩：

1. 连续跳过的荷叶数；
2. 正确率；
3. 输入速度，作为同分排序项。

## 7. 游戏二：警察抓小偷

### 7.1 核心循环

1. 城市地图扩展为多个可连续滚动的街区，每个街区是一个阶段；
2. 携带城市徽章的目标在前方持续移动，不再固定停在画面右侧；
3. 首个有效按键后警察、目标和计时同时启动；
4. 正确完成单词后警察缩短距离，长词、全对词和连续正确获得额外追赶距离；
5. 错误按键使警察短暂停顿，目标在停顿期间继续移动，但保留可追回的公平上限；
6. 每追回一次徽章或通过一个街区后进入下一阶段，地图主题、目标速度和词汇压力逐步提升；
7. 顶部同时显示迷你地图、当前街区、双方距离、目标速度和本局积分；
8. 目标超过最大安全距离、倒计时结束或孩子主动结束时结算；成功追回后可以继续追踪下一个更快目标，而不是立即结束整局。

### 7.2 叙事与表现

采用卡通、非暴力叙事，例如追回被拿走的书包或城市徽章。抓捕成功表现为小偷归还物品，失败表现为“线索还在，复习后再追一次”。

### 7.3 公平性

- 根据孩子表现进行轻度追赶平衡，避免早期两次错误导致整局必败；
- 目标基础速度随街区阶段提高，但单次错误造成的额外距离有上限，不能形成永久不可追回状态；
- 地图至少展示 4～6 个视觉街区并采用连续横向滚动；逻辑距离按时间和完成词驱动，不按帧率计算；
- 每个阶段保持可比较的目标速度、预计字符量和起始距离，阶段与起始检查点必须进入排行榜隔离键；
- 主要成绩为最高街区、追回徽章数、累计积分和最短追上时间；未成功时记录最短剩余距离。

## 7.4 游戏三：词语花园连连看

### 7.4.1 学习目标与核心循环

- 正式视觉采用原型 A“词语花园”，定位于“词义回忆”环节；
- 每张英文卡与唯一中文释义卡组成一对，孩子可使用鼠标、触控或按钮键盘焦点依次选择两张卡；
- 配对正确后两张卡消除并开出一朵花，同时记录正确词和共享词级记忆；
- 配对错误只清空当前选择并重置连击，不扣除已完成进度、不立即结束；
- 第一关 4 对且无倒计时，第二关 6 对并提供 120 秒，第三关 8 对并提供 90 秒；词汇难度分别覆盖 1～2、1～3、1～5 级；完成第一、二关后显示约 900 毫秒开花反馈并自动进入下一关，不要求孩子额外点击；
- 完成第三关、计时耗尽、孩子主动结束或页面退出时生成标准化 `RunResult`；完整通关才标记 `completed`。

### 7.4.2 无尽花园

- 正式交互采用无尽花园与普通学习双轨开局；首次完整通过普通三关后解锁无尽挑战，两种模式共享词级记忆但通过 `challengeMode` 与规则版本隔离排行榜；
- 无尽第 1 轮为 8 对和 100 秒，第 2 轮为 10 对和 95 秒，第 3 轮起保持 12 对；此后每轮缩短 5 秒，最低固定为 45 秒；
- 每轮完成后沿用约 900 毫秒开花反馈并自动进入下一轮，不弹出中间结算，也不要求额外点击；
- 本轮计时耗尽记为一次失败，并自动换牌重试当前轮次；只有完成本轮才提升轮次；累计第 3 次失败时立即冻结牌面、计时与输入并保存结算；
- 普通错配仍只清空选择并重置连击，不计入失败次数，避免一次误触消耗容错；
- 无尽成绩保存最高轮次、完成配对数、失败次数、错配词、积分和活跃用时；规则版本固定为独立的 `MATCH_ENDLESS_RULES_VERSION`。

### 7.4.3 共享系统与公平性

- 配对页不得自行散落状态规则，必须消费共享 `MatchEngine` / `useMatchSession` 输出的选择、配对、错配和重置事件；
- 本游戏不监听原始键盘事件，也不强制使用逐字输入专用 `TypingEngine`；按钮原生焦点提供 Enter/Space 等价操作；
- 开局前可选择外研版主题对齐的四年级、五年级、六年级或小升初衔接词库；牌面必须通过 `createWordSession` 从所选版本化词包随机生成，同一关不重复、错词优先、已稳定词降低权重，跨关尽量避免重复；
- 计时通过 `ActiveRunClock` 从首次有效选择开始，页面隐藏、阶段过渡和暂停不计入活跃用时；
- 通过 `ProgressStore` 保存 `gameId: 'match'`、`mode: 'recall'`、词包、规则版本、最高关卡、正确配对、错配词 ID、连击和积分；页面不得直接读写 IndexedDB；
- 配对成绩按游戏、模式、词包、难度、速度和规则版本隔离；外观、屏幕尺寸和帧率不得提供成绩优势；
- 正确、错误、过关和结算音效统一通过 Audio Service，且均有文字、颜色之外的形状或动画反馈并支持关闭声音。

## 7.5 游戏四：字母小火车

### 7.5.1 学习目标与核心循环

- 正式视觉采用原型 B“字母小火车”，定位于词义回忆到独立拼写之间的过渡训练；内部稳定 `gameId` 预定为 `spell`，正式接入数据模型时不得因视觉命名改变；
- 每词始终先显示中文释义，孩子根据当前提示车站看到完整、部分、少量或零个乱序英文字母，再使用实体键盘逐字拼出完整英文；
- 乱序提示必须保留目标单词真实字母及重复次数，不加入无关字母；存在不同排列时，完整提示结果不得与原词顺序相同，部分提示只减少可见字母，不缩短需要输入的答案；
- 正确字母依次装满对应车厢；错误字母不写入答案、不清空已正确前缀，只让当前车厢出现温和视觉反馈；
- 完整拼对后播放单词读音和发车反馈，等待约 700 毫秒自动进入下一词，不要求额外点击；孩子主动结束或离开页面时保存当前练习结果；
- 标准学习局固定完成四座车站、每站 4 词，共 16 词；完成第 4 站第 4 词保存 `completed: true`，主动结束或中途离开保存 `completed: false`，未完成的当前词进入后续复习。

### 7.5.2 四座提示车站

- 每完成 4 个单词提升一站，本局难度只升不降，最高停留在第 4 站；升级只改变乱序字母提示量，首个版本不叠加倒计时、生命值或额外速度压力；
- 第 1 站“满载车站”显示 100% 乱序字母；第 2 站“轻装车站”显示约 70%；第 3 站“记忆车站”显示约 35%；第 4 站“独立出发”隐藏全部字母，只看中文输入英文；
- 可见字母数按单词长度取整，并保证常见 3 字母以上单词在前三站总体单调减少；例如 7 字母词依次显示 7、5、2、0 个字母；
- 同一单词连续 2 次错键后，仅对当前单词临时增加 1 个隐藏字母作为救援提示；不得直接公布完整答案，也不得永久降低当前站级；
- 四年级、五年级、六年级和小升初四档词库均可开局选择；选词继续使用共享随机队列，单局尽量不重复、错词优先、近期熟练词降权。

### 7.5.3 共享系统、结算与公平性

- 页面必须消费共享 `TypingEngine` / `useTypingSession` 的标准逐字事件，禁止直接监听原始 `keydown`；列车、车厢和轨道动画只消费输入状态，不拥有业务判定；
- `ActiveRunClock` 从首个有效字母开始，页面隐藏、暂停、发车过渡和阶段提示不计入活跃用时；
- `ProgressStore` 后续保存 `gameId: 'spell'`、`mode: 'recall'`、词包、提示站级、规则版本、正确词与字符、错键、最高站级、连击、活跃时间、错词 ID 和是否触发救援提示；
- 排行榜按游戏、模式、词包、提示难度、速度等级和独立 `SPELL_RULES_VERSION` 隔离；完整提示成绩不得与零提示成绩混排；
- 正确字母、错误字母、整词完成、车站升级、暂停和结算都必须提供视觉反馈；音效和发音统一通过 Audio Service，支持静音及 `prefers-reduced-motion` 静态降级。

## 8. 共享学习系统

### 8.1 输入引擎

统一处理：

- 键盘焦点与浏览器快捷键冲突；
- 逐字正确/错误判定；
- Backspace 修正；
- 大小写和标点规则；
- 连击、准确率、字符速度和 WPM；
- 错误字母与混淆键统计；
- 页面失焦时自动暂停。

逐字输入游戏不得直接监听原始键盘事件，必须消费 Typing Engine 输出的标准事件；词义配对游戏消费 Match Engine，并通过原生按钮焦点获得键盘等价操作。

### 8.2 词库与复习

每个词条至少包含：

- 稳定 ID；
- 英文拼写；
- 中文释义；
- 年级、教材、册别和单元；
- 难度与标签；
- 图片和读音引用；
- 熟练度、最近练习时间和错误次数。

复习规则：

- 每局开始时由共享选词服务从完整词库生成随机队列，同一队列内不重复；游戏页不得按固定数组顺序自行取词；
- 最近一局出现过且已稳定掌握的词降低权重，避免连续开局重复；从未练习的词获得探索权重；
- 错误按键、单词超时或未完成会写入该词的本地记忆，并提高后续局的复习权重；
- 错词在后续随机队列中优先出现，但不得连续重复或占满整局；
- 一个错词连续两次无错完成后移出“待复习”，历史错误次数仍保留；
- 选词记忆至少记录稳定词条 ID、累计完成次数、错误次数、连续无错次数、最近练习与最近错误时间；
- 错题本正式采用原型 C“双层混合”：儿童层显示最多三个鼓励式待复习词，不展示错误次数；家长明细按需展开，展示错误、完成、连续无错和最近练习数据；
- 错题本的复习按钮进入共享随机选词游戏，待复习词按既有配额优先进入队列，不建立绕过共享选词服务的独立固定词序；
- 结算页优先复习 3～5 个最弱词；
- 熟练词逐步延长出现间隔；
- 支持按教材单元、易错词和自定义列表开局；
- MVP 支持 CSV 导入，后续可增加教材照片识别，但识别结果必须由家长确认。

### 8.2.1 外研版主题对齐分级词库

- 当前教材方向确认为外研版；在未逐单元核对具体册次前，词库名称使用“外研版主题对齐扩展词库”，不宣称为出版社官方逐字清单；
- 首批总量为 400 个不重复英文词：四年级 160 词、五年级进阶 80 词、六年级挑战 80 词、小升初衔接 80 词；
- 四档分别使用 `fltrp-grade4-v1`、`fltrp-grade5-v1`、`fltrp-grade6-v1`、`fltrp-junior-v1`，游戏开局选择后进入排行榜隔离键；
- 旧 `exp-*` 词条 ID 继续保留，已有学习记忆和历史成绩不丢失；所有等级继续共享错词记忆结构，但只在当前所选词包内生成牌面；
- 后续取得具体外研版册次和单元表后，以新增版本词包校对，不在原 ID 下静默改变排行榜词汇范围。

### 8.3 自适应难度

难度服务综合最近若干局的：

- 正确率；
- 中位字符速度；
- 单词长度；
- 首次反应时间；
- 连续错误数；
- 是否依赖键位提示。

推荐规则：正确率持续达到 92% 以上再提速；低于 80% 时减速或恢复提示，不通过突然加速制造难度。

阶段模式区分：

- 普通学习局继续使用上述自适应规则，保护新手体验；
- 已解锁的无尽挑战以“本局只升不降”为规则，阶段提升可预测且提前提示；
- 完成一个阶段后立即继续，升级横幅不暂停倒计时、不弹出结算页；
- 下一局推荐起点由历史最高阶段、近期正确率和字符速度共同决定，但始终提供“从热身开始”。

### 8.4 进度、积分与奖励

积分分为两层，不能混用：

- `score` 是单局竞技分，用于同规则排行榜，不可消费；
- `adventurePoints` 是本地累计奖励积分，可兑换外观或提交家庭奖励申请；
- 积分只在标准化 `RunResult` 成功保存后由 `ProgressStore` 按唯一 run ID 结算，刷新和重复保存不得重复发放；
- 基础积分来自完成单词、通过阶段、追回徽章、正确率和首次达到个人新纪录；错误不扣积分，也不设置负分；
- 高阶段和无提示模式提供透明倍率，结算页必须展示“基础分 + 阶段奖励 + 准确奖励”的来源；
- 为避免无意义刷分，积分至少要求有效输入和最低活跃时长；具体阈值进入规则版本并由测试固定。

虚拟奖励：

- 青蛙外观、荷叶主题、警帽、城市背景、徽章、贴纸和冒险地图装饰；
- 为增加长期目标，正式成本调整为：翡翠荷叶 3,000、落日城市 4,000、星光青蛙 5,000 积分；旧兑换记录和已拥有外观不受影响；
- 兑换后写入本地库存，可装备或切换，但不改变输入时间、生命数或排行榜公平性；
- 不提供付费购买、随机抽取、盲盒、限时焦虑或失败扣除。

家庭实物与现金愿望：

- 正式交互采用原型 A 的儿童端金额选择，并吸收原型 C 的家长汇率、每周预算和生产支付边界；
- 新练习本和彩色笔成本分别调整为 12,000 和 10,000 积分；家长仍可创建亲子活动等自定义奖励；
- 现金愿望默认按 1,000 积分兑换 1 元，家长可在 800～2,000 积分/元范围内调整汇率；每周现金预算默认 10 元，可在 0～50 元范围内调整；
- 儿童只能提交整数元申请，金额受可用积分和本周剩余预算共同限制；申请后预留积分，家长拒绝则完整释放；
- 当前本地版本由家长线下兑现，批准后扣除积分并标记已兑现，不收集儿童收款账户；实际收款人只允许是家长；
- 未来真实支付必须新增服务端权威积分账本、家长身份与授权、幂等转账订单、回调验签、主动查单和资金对账；浏览器不得持有商户私钥或直接调用转账 API；
- 真实支付状态按 `requested → approved → funds_locked → submitted → processing → fulfilled/failed` 推进；只有支付终态成功才正式扣分，失败时释放锁定积分；
- 系统不自动购买或配送实物，不把家庭奖励宣传为必然承诺；兑换记录、积分余额、预算和支付状态通过 `ProgressStore` 接口管理，本地与未来服务端实现不得泄漏到页面。

避免用“断签惩罚”制造焦虑；中断后可以自然继续。

## 9. 音乐、音效与发音系统

### 9.1 音频目标

音频用于建立节奏、强化正确反馈和提升沉浸感，但不能成为完成任务的唯一线索。所有声音必须有视觉等价反馈，并可单独调节或关闭。

### 9.2 音频通道

统一音频服务至少包含四个通道：

| 通道 | 内容 | 建议默认音量 |
|---|---|---:|
| Music | 首页及游戏循环背景音乐 | 35% |
| SFX | 正确、错误、跳跃、追赶、成功失败 | 70% |
| Voice | 单词发音与后续引导语音 | 100% |
| UI | 按钮、切换、弹窗等界面反馈 | 50% |

设置应分别持久化。全局静音后仍保留视觉反馈。

### 9.3 首批音频清单

| 音频 ID | 场景 | 设计要求 |
|---|---|---|
| `music.menu` | 冒险地图 | 明亮、轻松、可无缝循环 |
| `music.frog` | 青蛙游戏 | 活泼、自然、水边氛围，节奏不过快 |
| `music.chase` | 追逐游戏 | 有行动感但不紧张恐怖 |
| `run.success` | 通关/抓捕成功 | 2～4 秒庆祝短曲，不刺耳 |
| `run.failure` | 累计三次失败/整局超时 | 1～2 秒温和结束音，不羞辱 |
| `level.up` | 难度提升 | 清晰、短促、积极 |
| `combo.up` | 连击里程碑 | 随连击轻微增强，不每字触发 |
| `typing.correct` | 正确按键 | 极短、柔和，可连续播放 |
| `typing.wrong` | 错误按键 | 可辨识但不尖锐 |
| `frog.jump` | 青蛙起跳/落地 | 与动画时点同步 |
| `frog.rescue` | 荷叶错过 | 泡泡或小船救援音 |
| `chase.step` | 警察接近 | 只在单词完成后触发 |
| `chase.catch` | 成功追上 | 与成功短曲衔接 |
| `spell.depart` | 单词拼对后发车 | 轻快短促，与 700 毫秒自动过渡同步 |
| `word.pronounce` | 单词发音 | 清晰英语发音，可重复播放 |
| `ui.click` | 主要按钮 | 轻量、统一 |

### 9.4 播放规则

- 浏览器首次用户点击或按键后才解锁音频，不能依赖自动播放。
- 场景切换时背景音乐交叉淡入淡出，避免突然中断。
- 播放单词发音时，背景音乐自动降低约 50%，结束后恢复。
- 页面进入后台或游戏暂停时暂停背景音乐和计时。
- 高频正确音效需要限制并发，避免连续输入产生爆音。
- 成功、失败和升级短曲优先于普通音效，但不能盖住单词发音。
- 加载失败时游戏继续运行，不因音频资源失败阻塞开局。

### 9.5 音频素材授权

可考虑的现成素材来源包括 Pixabay、Mixkit、OpenGameArt 和 Freesound，但每个素材必须在下载时单独核验授权。

允许：

- 自制或委托制作；
- 购买且许可 Web 游戏分发的素材；
- CC0；
- 允许商业使用的 CC-BY，并完整记录署名。

默认不采用：

- 仅限非商业使用的许可；
- 来源、作者或授权不明确的转载；
- 从金山打字通或其他商业游戏提取的音乐、音效；
- 会强制整个项目采用相同许可的素材，除非单独评审确认。

未来每个音频素材必须登记：

- 内部 ID；
- 文件路径；
- 标题与作者；
- 来源 URL；
- 授权类型与授权页面；
- 获取日期；
- 是否要求署名；
- 文件校验值；
- 循环点和推荐音量。

正式加入素材时，同时维护音频清单和 `CREDITS.md`。未登记素材不得进入发布包。

### 9.6 音频实现约束

- 所有游戏通过共享 Audio Service 按音频 ID 播放，不引用具体文件名。
- 音频映射由 manifest 管理，主题包可以替换素材而无需修改游戏逻辑。
- 背景音乐提供通用格式和兼容回退，优先控制首屏下载体积。
- 单词发音 MVP 可采用浏览器语音能力作为回退；正式教材词库优先使用质量一致、授权明确的音频。
- 音频服务必须支持预加载、缓存、音量、静音、淡入淡出、语音 ducking 和资源缺失回退。

## 10. 排行榜与成绩模型

榜单隔离键：

`gameId + mode + wordPackId + difficulty + speedTier + rulesVersion`

所有成绩至少记录：

- 正确词数和字符数；
- 错误次数和正确率；
- 连续正确数；
- 首个有效输入后的活跃用时，以及单独累计的暂停时间；
- 词库、模式、难度、速度和规则版本；
- 挑战模式、起始阶段、最高阶段、追回徽章数与本局奖励积分；
- 奖励结算规则版本和防重复结算所需的唯一 run ID；
- 是否使用完整键位提示。

优先展示：

1. 个人历史最佳；
2. 与自己的上次成绩比较；
3. 本机家庭排行榜。

在线榜单以后再做，并使用服务端校验、匿名昵称和家长控制。

## 11. 技术基线

编码阶段建议采用：

- React + TypeScript + Vite：应用壳、页面和家长中心；
- Phaser 3：2D 场景、角色动画、荷叶运动和追逐表现；
- IndexedDB：词库、设置、学习进度和本地成绩；
- PWA：安装、离线与资源缓存；
- 共享服务：Typing Engine、Match Engine、Word Bank、Adaptive Difficulty、Progress Store、Leaderboard、Audio Service。

后端不进入首个 MVP。需要跨设备同步或在线排行榜时，通过接口层接入，游戏模块不直接依赖具体云服务。

## 12. 游戏扩展契约

未来每个新游戏都必须提供标准模块描述：

- 唯一 `gameId`；
- 展示名称、地图区域、支持模式；
- 支持的难度与速度等级；
- 所需角色、场景和音频 ID；
- 开局、暂停、恢复、结算和销毁生命周期；
- 对标准输入事件的响应；
- 标准化 Run Result 输出；
- 公平性规则和榜单隔离字段。

新游戏禁止：

- 直接读写 IndexedDB；
- 直接监听原始 `keydown`；
- 自建一套词库、音量设置或排行榜；
- 将帧率、屏幕尺寸直接换算成成绩优势；
- 把声音作为唯一提示；
- 未登记授权的图片、字体、音乐和音效。

新增游戏流程：

1. 写清学习目标和核心循环；
2. 提供 2～3 套视觉原型；
3. 说明复用哪些共享系统；
4. 定义输入事件、音频事件和结算数据；
5. 完成可玩的纵向切片；
6. 做新用户可发现性和儿童可理解性评审；
7. 验证动画、音频、暂停、低性能设备和离线行为；
8. 更新本文件的地图、音频清单和决策记录。

## 13. MVP 范围

第一版包含：

- 桌面/笔记本真实键盘优先；
- 本地儿童档案；
- 键位热身；
- 青蛙跳荷叶；
- 警察抓小偷；
- 词语花园英中连连看与四档可选分级词库；
- 字母小火车四站渐隐拼写训练与四档可选分级词库；
- 键位、抄写和中文回忆模式；
- 教材词库及 CSV 自定义导入；
- 本地进度、个人最佳和家庭排行榜；
- 背景音乐、成功/失败短曲、核心动作音效和单词发音；
- 独立音量、全局静音和音频授权清单；
- PWA 与离线运行。

第一版不包含：

- 实时多人；
- 公开社交和聊天；
- 全球排行榜；
- 付费、广告或抽卡；
- 复杂账号系统；
- 未经家长确认的 AI 自动词库。

## 14. 推荐实施顺序

1. 确认教材版本、当前册别和首批单词；
2. 制作 3 套视觉原型，并附带音乐氛围方向；
3. 初始化 Web 工程和共享领域模型；
4. 完成键位热身与输入引擎；
5. 完成青蛙游戏纵向切片；
6. 接入自适应难度、音频和本地成绩；
7. 完成警察游戏；
8. 完成单词本、家长中心和家庭排行榜；
9. 做动画录屏、音频验证和新用户可用性评审；
10. 构建 PWA 并验证离线模式。

## 15. 验收标准

每个可发布游戏至少满足：

- 新用户无需阅读外部说明即可开始；
- 输入框始终保持正确焦点，页面失焦会暂停；
- 正确、错误、完成和失败都有视觉反馈；
- 关闭声音后不丢失任何必要信息；
- 背景音乐、音效和语音可独立调节；
- 音频资源失败不会阻止游戏；
- 相同规则下成绩可比较，不同规则下榜单隔离；
- 学习进度跨游戏共享；
- 主流普通笔记本保持流畅，速度不受帧率影响；
- 刷新或离线后本地进度不丢失；
- 所有发布素材有明确授权记录；
- 静态页面用截图验证，动画和完整游戏流程用录屏验证；
- 由不了解项目的新用户完成一次可发现性和可理解性评审。

## 16. 参考产品

- 金山打字游戏《生死时速》：警察追小偷主题参考；
- Keyboard Jump：输入单词后跳到下一平台的核心机制；
- BBC Dance Mat Typing：分键位、分阶段、短时练习；
- TypeTastic：彩色键盘区域和儿童递进教学；
- Nitro Type：输入驱动追逐、外观奖励和长期目标；
- typing-english：重复输入、速度阈值和单词记忆结合。

参考机制，不复制其美术、音乐、文案、代码或受版权保护的具体素材。

## 17. 待确认项

- 外研版具体册别、上下册和当前单元，以便后续建立经过逐单元核对的新版本词包；
- 是否需要在当前 400 词基础上继续扩充初中词库；
- 后续是否允许家长调整失败上限；MVP 与当前排行榜规则固定为 3 次；
- 家庭成员是否需要多个本地档案；
- 第一批背景音乐和成功/失败音效的候选素材；

## 18. 决策记录

| 日期 | 决策 |
|---|---|
| 2026-09-03 | 产品采用共享学习系统下的多游戏集合，而非两个独立页面。 |
| 2026-09-03 | 首批游戏为青蛙跳荷叶和警察抓小偷。 |
| 2026-09-03 | MVP 本地优先，不引入账号和后端。 |
| 2026-09-03 | 学习路径覆盖键位、抄写、词义回忆和听写。 |
| 2026-09-03 | 音乐、音效和单词发音纳入共享 Audio Service。 |
| 2026-09-03 | 允许使用授权明确的现成音频，但禁止提取商业游戏素材。 |
| 2026-09-03 | 后续新增功能必须遵循本文件的共享服务和扩展契约。 |
| 2026-09-03 | 儿童主界面采用原型 A“绘本冒险岛”，家长中心采用原型 C“现代学习仪表盘”。 |
| 2026-09-03 | MVP 明确以桌面或笔记本实体键盘为首要输入设备，屏幕键盘仅作不可点击的键位提示图。 |
| 2026-09-03 | 成绩用时从首个有效输入开始，仅累计活跃游戏时间；后台或暂停时间单独记录。 |
| 2026-09-03 | 个人最佳与家庭排行均只在完整榜单隔离键内比较，摘要默认展示最近使用规则组的最佳成绩。 |
| 2026-09-03 | 正式 PWA 同时提供 SVG、192×192、512×512、maskable 和 Apple Touch PNG 图标。 |
| 2026-09-03 | 词汇游戏改用共享随机选词队列：单局不重复、最近熟练词降权、错词优先复习；词级错误与连续无错记录经 ProgressStore 本地持久化，错词连续两次无错后退出待复习。 |
| 2026-09-03 | 正式错题本采用原型 C“双层混合”：儿童看到最多三词的鼓励式复习路线，家长明细按需展开；复习仍进入共享随机队列，不建立固定错词序列。 |
| 2026-09-04 | 用户确认加入阶段成长：青蛙首次通关后解锁无尽挑战并持续提速，城市追踪扩展为多街区移动目标；新增本地累计积分、虚拟外观兑换和家长确认的线下实物奖励，正式交互待三套原型确认。 |
| 2026-09-04 | 阶段成长与奖励正式界面采用原型 C“双轨成长与家庭奖励柜”：普通学习与无尽挑战分轨，共享单词记忆但隔离排行榜；虚拟外观即时兑换，实物奖励由家长确认。 |
| 2026-09-04 | 原型 C 已完成正式实现：青蛙每 8 词无打断升级并支持已解锁检查点的无尽挑战；城市目标按真实时间持续移动并可连续追回徽章；成长岛奖励柜、虚拟外观装备及家长确认的家庭奖励流程已接入 IndexedDB v2，并通过全量测试、覆盖率、PWA 构建和真实 Chrome 桌面/窄屏验证。 |
| 2026-09-04 | 将原 12 词体验集扩充并保留旧词稳定 ID；新增“英文—中文连连看”候选游戏，先提供词语花园、星桥连线、词卡秘境三套鼠标/触控原型，用户确认后才进入正式实现。 |
| 2026-09-04 | 用户确认采用原型 A“词语花园”正式实现第三个游戏：三关分别为 4/6/8 对，错误不扣已完成进度；新增共享 Match Engine 作为非逐字输入游戏的标准事件边界，并继续复用随机词库、ActiveRunClock、ProgressStore、积分、排行榜和 Audio Service。 |
| 2026-09-04 | 用户确认当前教材为外研版，并要求覆盖至六年级和未来初中衔接且可选等级；首批采用 400 词四档扩展词库，词语花园开局可选四年级、五年级、六年级或小升初，成绩按独立词包 ID 隔离。 |
| 2026-09-04 | 原型 A“词语花园连连看”已完成正式实现：共享 Match Engine、4/6/8 对递进、分级词库选择、错词记忆、活跃计时、积分、独立排行榜与本地持久化全部接入；400 词唯一性、121 项测试、覆盖率门槛、PWA 构建及真实 Chrome 桌面/390px 视觉审阅均通过。 |
| 2026-09-04 | 用户反馈青蛙落到移动荷叶后必须持续跟随荷叶，避免角色与承载面脱离；普通学习和无尽挑战统一显示失败计数并固定最多 3 次，第 3 次失败立即停止结算。“无尽”只表示无固定通关词数，不表示无限容错。 |
| 2026-09-04 | 青蛙规则升级为 `1.4.0`：跳跃终点实时追踪移动荷叶，落地后每帧同步所在荷叶坐标；HUD 显示“失败 x/3”，普通与无尽模式均在第 3 次失败时保存并结束。纯运动测试、场景接入契约、无尽失败流程、126 项全量测试、覆盖率与生产构建均通过。 |
| 2026-09-04 | 用户要求词语花园完成第一、二关后自动进入下一关，不再点击确认；青蛙路线固定由下往上，到达最高荷叶后自动返回底部并重新向上，不能从顶部反向跳回。Match 规则版本随固定自动过关节奏升级。 |
| 2026-09-04 | 自动流程已实现：Match `1.1.0` 在前两关完成后暂停活跃计时、展示 900 毫秒开花提示并自动发牌；青蛙通过单向路线状态与回岸动画执行“底部 → 顶部 → 底部”。128 项测试、覆盖率门槛、PWA 构建及依赖审计通过。 |
| 2026-09-07 | 用户确认连连看新增无尽模式，并在三套交互原型中选择 A“双轨平衡”界面，同时吸收 C 的家长汇率、每周预算和生产支付边界。 |
| 2026-09-07 | 无尽花园规则固定为 8/10/12 对递进、100 秒起每轮缩短 5 秒且最低 45 秒、超时累计三次结束；现金愿望默认 1,000 积分兑 1 元，当前先由家长线下兑现，真实转账必须经过服务端权威账本与家长实名授权。 |
| 2026-09-07 | 用户确认第四个游戏采用原型 B“字母小火车”：中文在上、乱序英文字母提示在下，孩子逐字输入；每 4 词依次从完整乱序、部分乱序、少量提示升级到只看中文完整拼写。 |
| 2026-09-07 | 字母小火车首版不加入倒计时或生命值；错键保留正确前缀，连续 2 次错误仅临时恢复 1 个隐藏字母，整词正确后等待约 700 毫秒自动发车并进入下一词。 |
| 2026-09-07 | 字母小火车标准学习局固定为四站共 16 词，完整通过后结算；正式页面接入四档共享词库、Typing Session、ActiveRunClock、ProgressStore、排行榜隔离、积分与 AudioService，并加入冒险地图入口。 |
